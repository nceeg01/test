import React,{useMemo,useState}from"react";import{createRoot}from"react-dom/client";import{book,retrieve,slots}from"./domain";import"./style.css";
const customers=[{id:"C-1001",topic:"New inquiry",detail:"Interested in scheduling a service appointment."},{id:"C-1002",topic:"Follow up",detail:"Requested a follow-up about availability."},{id:"C-1003",topic:"Reschedule request",detail:"Needs a different appointment window."},{id:"C-1004",topic:"Product question",detail:"Asked a question outside the demo knowledge base."}];
const seed=["Call started (simulated) — no external call was placed.","Agent — I can help schedule a service appointment.","Customer (simulated) — Morning would be best.","Availability shown — four synthetic slots found in America/Chicago."];
function App(){const[id,setId]=useState("C-1001"),[events,setEvents]=useState([]),[selected,setSelected]=useState("10"),[sync,setSync]=useState("idle"),[bookings,setBookings]=useState(()=>JSON.parse(localStorage.getItem("voice-demo-bookings")||"[]"));const customer=customers.find(x=>x.id===id),context=useMemo(()=>retrieve(customer.detail),[customer]);
function reserve(){try{const next=book(bookings,id,selected);setBookings(next);localStorage.setItem("voice-demo-bookings",JSON.stringify(next));setEvents(x=>[...x,`Appointment booked — ${slots.find(s=>s.id===selected).label}.`,"Call completed — booking persisted in this browser."]);setSync("synced")}catch(e){setSync("failed");setEvents(x=>[...x,e.message])}}
return <main><header><strong>Customer Service RAG Agent</strong><span>Synthetic demo mode</span><p>Voice scheduling and customer outreach</p><b>YC</b></header><div className="layout"><aside className="customers"><h2>Customers</h2><input aria-label="Search customers" placeholder="Search customers…"/>{customers.map(c=><button className={c.id===id?"active":""}onClick={()=>{setId(c.id);setEvents([])}}key={c.id}><strong>{c.id}</strong><small>{c.topic}</small></button>)}</aside><section className="call"><div className="customerHead"><div><h1>{customer.id}</h1><p>{customer.topic} · Synthetic record</p></div><em>● Ready to call</em></div><section className="context"><h2>Conversation context</h2><div><article><strong>Reason for contact</strong><p>{customer.detail}</p><strong>Knowledge retrieved</strong><p>{context}</p></article><article><strong>Suggested talking points</strong><ul><li>Confirm the customer goal</li><li>Offer available time slots</li><li>Repeat the time zone</li></ul></article></div></section><button className="start"onClick={()=>{setEvents(seed);setSync("idle")}}>☎ Start simulated call</button><small>No real calls or messages are made.</small><div className="eventHead"><h2>Call events</h2><span>{events.length?"● Simulation complete":"Not started"}</span></div><ol>{events.length?events.map((e,i)=><li key={e}><time>10:{14+i*3} AM</time><span>{e}</span></li>):<li className="empty">Start the simulator to create a local event timeline.</li>}</ol></section><aside className="booking"><h2>Availability</h2><p>America/Chicago (CDT)</p><h3>Monday, September 14, 2026</h3>{slots.map(s=><label className={selected===s.id?"selected":""}key={s.id}><input type="radio"name="slot"checked={selected===s.id}onChange={()=>setSelected(s.id)}/><span><strong>{s.label}</strong><small>{bookings.some(b=>b.slotId===s.id)?"Booked":"Available"}</small></span></label>)}<button className="book"onClick={reserve}>Book appointment</button><section className={"sync "+sync}><h2>Sync status</h2>{sync==="synced"?<><strong>✓ Saved locally</strong><p>The booking persists after refresh.</p></>:sync==="failed"?<><strong>⚠ Booking conflict</strong><p>No duplicate booking was created.</p><button onClick={()=>setSync("idle")}>Retry sync</button></>:<p>Book an appointment to test persistence and contention handling.</p>}</section></aside></div></main>}createRoot(document.getElementById("root")).render(<React.StrictMode><App/></React.StrictMode>);